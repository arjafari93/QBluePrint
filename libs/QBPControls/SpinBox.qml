// qml/controls/SpinBox.qml
import QtQuick
import QtQuick.Controls as T

T.SpinBox {
    id: control
    // implicitWidth: Math.max(120, implicitBackgroundWidth + leftInset + rightInset)
    // implicitHeight: Math.max(36, implicitBackgroundHeight + topInset + bottomInset)
    // background: Rectangle{
    //     color: "transparent"
    //     border.color: "transparent"
    // }
}
